<Window x:Class="FileEncryptor.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Encryptor" Height="300" Width="380"
        WindowStartupLocation="CenterScreen"
        Background="#FF0E0E1A"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="&#x1F510; 文件加密/解密" FontSize="16" FontWeight="Bold"
                   Foreground="#FFE0E0E0" Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1" FontSize="12" Foreground="#FF888899" Margin="0,0,0,14"
                   Text="{Binding FileInfo, RelativeSource={RelativeSource AncestorType=Window}}"/>

        <TextBlock Grid.Row="2" Text="输入密码" FontSize="11" Foreground="#FF888899" Margin="0,0,0,5"/>
        <PasswordBox x:Name="PasswordBoxMain" Grid.Row="3"
                     Background="#FF0D0D1A" Foreground="#FFE0E0E0"
                     BorderBrush="#FF2A2A40" BorderThickness="1"
                     Padding="12,10" FontSize="13" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="确认密码" FontSize="11" Foreground="#FF888899" Margin="0,0,0,5"
                   x:Name="ConfirmLabel"/>
        <PasswordBox x:Name="PasswordBoxConfirm" Grid.Row="4"
                     Background="#FF0D0D1A" Foreground="#FFE0E0E0"
                     BorderBrush="#FF2A2A40" BorderThickness="1"
                     Padding="12,10" FontSize="13" Margin="0,24,0,0"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Click="Cancel_Click"
                    Background="#FFFF4757" Foreground="White"
                    BorderThickness="0" Padding="18,7" Margin="0,0,8,0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="OkButton" Content="确定" Click="Ok_Click"
                    Background="#FF667eea" Foreground="White"
                    BorderThickness="0" Padding="18,7"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
